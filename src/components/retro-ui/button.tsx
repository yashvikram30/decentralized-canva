import { Button } from 'pixel-retroui';

export function RetroButton() {
  return (
    <Button
      bg="#ffffff"
      textColor="#000000"
      borderColor="#000000"
      shadow="#000000"
    >
      Themed Button
    </Button>
  );
}